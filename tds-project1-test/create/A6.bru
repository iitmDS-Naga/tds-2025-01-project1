meta {
  name: A6
  type: http
  seq: 6
}

post {
  url: http://localhost:8000/run?task="Find all Markdown (.md) files in /data/docs/. Extract the first H1 heading from each file and create an index file at /data/docs/index.json"
  body: none
  auth: none
}

params:query {
  task: "Find all Markdown (.md) files in /data/docs/. Extract the first H1 heading from each file and create an index file at /data/docs/index.json"
}
