meta {
  name: A4
  type: http
  seq: 4
}

post {
  url: http://localhost:8000/run?task="Sort the array of contacts in /data/contacts.json by last_name, then first_name, and write the result to /data/contacts-sorted.json"
  body: none
  auth: none
}

params:query {
  task: "Sort the array of contacts in /data/contacts.json by last_name, then first_name, and write the result to /data/contacts-sorted.json"
}
